  1: We found some dead code. If it's not an error, remove the code. OK
  2: Error return value of `(*encoding/json.Encoder).Encode` is not checked OK
  4: We found some unnecessary type casting (casting from a type to the same type), e.g.: `var x int` and `p = int(x)`
  1: `r_streamPhoto` is unused OK
  3: Check all variable assignments. Some variables receive a value but then you don't use them, or you overwrite them before using the value. OK
